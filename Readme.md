```
Usage of servefile:
  -e	(shorthand) Include ipv6 addresses, default: false
  -enable_ipv6
    	Include ipv6 addresses, default: false
  -f string
    	(shorthand) file to serve
  -file string
    	file to serve
  -p	Print urls to terminal, default: false
  -port int
    	port to serve the file on, default: 6066 (default 6066)
```
